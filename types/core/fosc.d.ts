export = fosc;
/**
 *
 * @param {number[]} source
 * @param {number} period
 * @param {number} [size]
 * @returns
 */
declare function fosc(source: number[], period: number, size?: number): Promise<number[]>;
