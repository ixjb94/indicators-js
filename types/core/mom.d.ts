export = mom;
/**
 *
 * @param {number[]} source
 * @param {number} period
 * @param {number} [size]
 * @returns
 */
declare function mom(source: number[], period: number, size?: number): Promise<number[]>;
