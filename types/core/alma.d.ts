export = alma;
/**
 *
 * @param {number[]} source
 * @param {number} period
 * @param {number} offset
 * @param {number} sigma
 * @param {number} [size]
 * @returns
 */
declare function alma(source: number[], period: number, offset: number, sigma: number, size?: number): number[];
