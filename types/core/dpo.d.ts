export = dpo;
/**
 *
 * @param {number[]} source
 * @param {number} period
 * @param {number} [size]
 * @returns
 */
declare function dpo(source: number[], period: number, size?: number): Promise<number[]>;
