export = vwma;
/**
 *
 * @param {number[]} source
 * @param {number[]} volume
 * @param {number} period
 * @param {number} [size]
 * @returns
 */
declare function vwma(source: number[], volume: number[], period: number, size?: number): number[];
